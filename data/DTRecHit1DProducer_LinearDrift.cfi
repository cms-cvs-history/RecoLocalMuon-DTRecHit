# Module for rechit building of simulated digis using the constant 
# drift velocity over the entire cell

# The reconstruction algo and its parameter set
include "RecoLocalMuon/DTRecHit/data/DTLinearDriftAlgo.cfi"

module DTRecHit1DProducer = DTRecHitProducer {

                untracked bool debug = false
                string dtDigiLabel = 'muondtdigi'      

		# The reconstruction algo and its parameter set
		using DTLinearDriftAlgo
        }
