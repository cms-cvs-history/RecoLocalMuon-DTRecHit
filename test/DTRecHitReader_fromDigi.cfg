process DTRecHitReaderfromDigi = {
	source = PoolSource {
     		 untracked vstring fileNames = {'file:/afs/cern.ch/cms/Physics/muon/CMSSW/DT/samples/2006-06-01/muondigi.root'}
		 #untracked string catalog = "file:PoolFileCatalog_data.xml "
		 untracked int32 maxEvents = 1000
     		 untracked uint32 debugVebosity = 10
     		 untracked bool   debugFlag     = true
		}
 
        include "MagneticField/Engine/data/volumeBasedMagneticField.cfi"
        include "Geometry/MuonCommonData/data/muonIdealGeometryXML.cfi"
        include "Geometry/DTGeometry/data/dtGeometry.cfi"


	# The module for 1D DT RecHit building
	include "RecoLocalMuon/DTRecHit/data/dt1DRecHits_LinearDrift.cfi"


	module DTRecHit1DReader = DTRecHitReader{
		untracked bool debug = true
		untracked string simHitLabel = 'g4SimHits'
		untracked string recHitLabel = 'dt1DRecHits'
		untracked string rootFileName = "DTRecHitPlots.root"
	}	


  
        path p = {dt1DRecHits, DTRecHit1DReader}
}

