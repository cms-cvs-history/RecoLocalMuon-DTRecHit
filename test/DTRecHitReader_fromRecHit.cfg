process DTRecHitReaderFromRecHit = {
	source = PoolSource {
     		 untracked vstring fileNames = {'file:/afs/cern.ch/cms/Physics/muon/CMSSW/DT/samples/2006-06-04/MuonRecHit.root'}
     		 untracked int32 maxEvents = -1
     		 untracked uint32 debugVebosity = 10
     		 untracked bool   debugFlag     = true
		}
 
        include "Geometry/MuonCommonData/data/muonIdealGeometryXML.cfi"
        include "Geometry/DTGeometry/data/dtGeometry.cfi"


	module DTRecHit1DReader = DTRecHitReader{
		# Switch on/off the verbosity
		untracked bool debug = true
		# Lable to retrieve SimHits from the event
		untracked string simHitLabel = 'g4SimHits'
		# Lable to retrieve RecHits from the event
		untracked string recHitLabel = 'dt1DRecHits'
		# Name of the root file which will contain the histos
		untracked string rootFileName = "DTRecHitPlots.root"
	}	

	 
	path p = {DTRecHit1DReader}

}

