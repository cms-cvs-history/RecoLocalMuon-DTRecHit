process PROD = {
	source = PoolSource {
     		 untracked vstring fileNames = {'file:simevent.root'}
     		 untracked int32 maxEvents = 1000
     		 untracked uint32 debugVebosity = 10
     		 untracked bool   debugFlag     = true
		}
 
	module mix = MixingModule {         
                secsource input = PoolRASource {
                                    #vstring fileName = {'simevent_pu.root'}
				   untracked vstring fileNames = {'/afs/cern.ch/user/u/uberthon/public/CMSSW_0_5_0_pre4/src/SimGeneral/MixingModule/test/data/simevent.root'}
                                   # bool random = true
                                   # bool no_reuse = true
                
	        string type = "poisson"
        	double averageNumber = 0
        	int32 minBunch =xxx
        	int32 maxBunch =xxx
		int32 seed = 1234567
               }
        	int32 bunchspace =25
               }

        include "MagneticField/Engine/data/volumeBasedMagneticField.cfi"
        include "Geometry/MuonCommonData/data/muonIdealGeometryXML.cfi"
        include "Geometry/DTGeometry/data/dtGeometry.cfi"

	include "SimMuon/DTDigitizer/data/muondtdigi.cfi"

 
	# The module for 1D DT RecHit building
	include "RecoLocalMuon/DTRecHit/data/dt1DRecHit_linearDrift.cfi"


	module rechitreader = DTRecHitReader{
		untracked bool debug = true
		untracked string simHitLabel = 'SimG4Object'
		untracked string recHitLabel = 'dtrechitbuilder'
		untracked string rootFileName = "DTRecHitPlots.root"
	}	

	path p = {mix,muondtdigi,dtrechitbuilder,rechitreader}

}
